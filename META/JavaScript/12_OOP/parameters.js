//  *** Parámetros por defecto ***

// Función sin parámetros por defecto establecidos:
function noDefaultParams(number) {
  console.log("Result:", number * number);
}
noDefaultParams(); // Result: NaN
