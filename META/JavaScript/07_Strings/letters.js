//  For loop over arrays

var letters = ["a", "b", "c"];

for (var i = 0; i < letters.length; i++) {
  console.log(letters[i]);
}

// ------------------------------------------
//  For loop over string

var letters = "abc";

for (var i = 0; i < letters.length; i++) {
  console.log(letters[i]);
}
