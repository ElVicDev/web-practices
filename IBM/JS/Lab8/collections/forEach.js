/* Para iterar a través de arreglos, hay un tipo especial de bucle for, forEach,
que se ejecuta para cada valor en el arreglo dado. */

let myArray = ["Jack", "Jill", 4, 5, true, "John"];
myArray.forEach((element) => {
  console.log(element);
});
