// * ------------------------
// * Matrices como parámetros
// * ------------------------

// Crea una función, getFirst(arr), que devuelva el primer elemento de la matriz
function getFirst(arr) {
  return arr[0];
}

let firstCard = getFirst([10, 2, 5]);

// Llámala con una matriz como argumento para verificar que funciona
console.log(firstCard);
