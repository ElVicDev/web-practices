// * Crea una variable, messageToUser,
// * que contiene el mensaje que hemos registrado

let username = "ElVicDev";
let message = "You have tree new notifications";

let messageToUser = message + ", " + username + "!";

console.log(messageToUser);
