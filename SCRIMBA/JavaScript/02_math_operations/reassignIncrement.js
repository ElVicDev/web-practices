/*  Crea una variable, bonusPoints. Inicialízala como 50. Auméntala a 100.
    Disminúyela a 25 y, finalmente, auméntala a 70.
    Registra el valor en console.log después de cada paso. */

let bonusPoints = 50;
console.log(bonusPoints);

bonusPoints = bonusPoints + 50;
console.log(bonusPoints);

bonusPoints = bonusPoints - 75;
console.log(bonusPoints);

bonusPoints = bonusPoints + 45;
console.log(bonusPoints);
