let dayOfMonth = 13;
let weekday = "Friday";

// Si es viernes 13, cierra la sesión con esta cara espeluznante: 😱
// Usa el operador lógico "AND" -> &&

if (dayOfMonth === 13 && weekday === "Friday") {
  console.log("😱");
}
